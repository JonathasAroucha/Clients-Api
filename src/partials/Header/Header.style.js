import { makeStyles } from '@material-ui/core/styles'


const useStyles = makeStyles(() => {
    return {
        title: {
            flexGrow: 1,
            marginLeft: 10,
        }
    }
})

export default useStyles